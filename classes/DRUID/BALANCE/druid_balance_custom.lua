SpellCoreRotations["DRUID_BALANCE_CUSTOM"] = function()
    print("🔥 DRUID_BALANCE_CUSTOM rotation initialized.")

    -- Out of Combat
    SpellCore.OOC_Rotation = {
        -- Example: { "BuffName", "Condition" }
    }

    -- Combat
    SpellCore.CombatRotation = {
        -- Example: { "SpellName", "Condition" }
    }
end
