SpellCoreRotations["WARLOCK_DEMONOLOGY_RAID"] = function()
    print("🔥 WARLOCK_DEMONOLOGY_RAID rotation initialized.")

    -- Out of Combat
    SpellCore.OOC_Rotation = {
        -- Example: { "BuffName", "Condition" }
    }

    -- Combat
    SpellCore.CombatRotation = {
        -- Example: { "SpellName", "Condition" }
    }
end
